language: php
before_install:
  - sudo apt-get update
  - sudo apt-get install zendframework-bin
before_script:
  - zf create project .
  - zf enable layout
  - wget --no-check-certificate https://github.com/muiku/h5bp-zendframework/tarball/zfint -O - | tar -xvz --strip 1
script: ant -buildfile public/build/build.xml
